{% extends 'base.html.twig' %}

{% block title 'Créer une option' %}

{% block body %}

<div class="container mt-4 mb-4">
    <h1>Créer une nouvelle option</h1>
    {{ form_start(form) }}
    {{ form_rest(form) }}
        <button class="btn btn-primary">Créer</button>
    {{ form_end(form) }}
{# {{ data.name }} #}
</div>
{% endblock %}
